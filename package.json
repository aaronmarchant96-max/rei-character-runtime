{
  "name": "rei-character-runtime",
  "version": "0.1.0",
  "private": false,
  "description": "Evidence-driven runtime for contextual, voiced game characters.",
  "type": "module",
  "scripts": {
    "demo": "node src/cli.js",
    "test": "node --test",
    "check": "node --check src/*.js && node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
